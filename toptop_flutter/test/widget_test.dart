import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds successfully', (WidgetTester tester) async {
    // Firebase cần mock cho test thực tế - placeholder test
    expect(true, isTrue);
  });
}
